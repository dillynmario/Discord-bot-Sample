#!/usr/bin/env node

const program = require('commander')
const helpOptions = require('./lib/core/vue/help')
const createCommand = require('./lib/core/vue/create')


helpOptions();
program.version(require('./package.json').version);

// 创建命令
createCommand();

// 解析终端指令
program.parse(process.argv);