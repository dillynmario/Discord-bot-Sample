/**helps */

const program = require('commander') 

const helpOptions = () => {
    // 定义可选参数
    program.option('-v --version', 'whyvue version')
    program.option('-d --dest <dest>', 'a destination folder')
    program.option('-f --framework <framework>', 'you framework')
}

module.exports = helpOptions